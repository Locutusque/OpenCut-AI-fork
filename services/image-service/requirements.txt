fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
aiofiles==24.1.0
Pillow>=10.0.0
# torch is installed from a backend-specific wheel index (CPU/CUDA/ROCm) by the
# Dockerfile / run-native.py, pinned to the 2.9 series; this line is stripped
# before the PyPI install (documentation of the expected version).
torch~=2.9.0
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
rembg[cpu]>=2.0.50
# numpy floor with prebuilt cp312+ wheels. Without it pip backtracks (chasing the
# numba/onnxruntime ranges pulled in by rembg) all the way down to numpy 1.25.x,
# which has no wheel for current Pythons and fails to build from source
# (setuptools/pkgutil.ImpImporter was removed in 3.12+). 1.26 is the first numpy
# with 3.12 wheels.
numpy>=1.26.0
