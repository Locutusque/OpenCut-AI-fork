# In-process CPU engine: HuggingFace Optimum + ONNX Runtime.
# Installs on top of the base deps.
-r requirements.txt
torch>=2.2.0
transformers>=4.44.0
optimum[onnxruntime]>=1.23.0
accelerate>=0.30.0
huggingface_hub>=0.23.0
# Optional: optimum-intel[openvino] for OpenVINOExecutionProvider on Intel CPUs.
